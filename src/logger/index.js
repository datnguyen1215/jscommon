import transports from './transports';
import createLogger from './create-logger';
import format from './format';

const index = { transports, createLogger, format };
export { transports, createLogger, format, index as default };
