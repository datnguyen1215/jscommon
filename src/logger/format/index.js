import combine from './combine';
import custom from './custom';
import timestamp from './timestamp';

const index = { combine, custom, timestamp };
export { combine, custom, timestamp, index as default };
