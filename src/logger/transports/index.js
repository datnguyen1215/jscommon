import ConsoleTransport from './console';
import NullTransport from './null';
import Transport from './base';

const index = { ConsoleTransport, Transport, NullTransport };
export { ConsoleTransport, Transport, NullTransport, index as default };
